/* ============================================================
   PulsarAds — Configuração
   Depois de publicar o backend no Render, cole aqui a URL dele
   (sem barra no final). Enquanto estiver vazio, o site funciona
   normalmente com PIX + código de ativação, e os botões de
   "Pagar com Mercado Pago" ficam escondidos.
   Ex.: window.PULSAR_BACKEND = "https://pulsarads-backend.onrender.com";
   ============================================================ */
window.PULSAR_BACKEND = "";
